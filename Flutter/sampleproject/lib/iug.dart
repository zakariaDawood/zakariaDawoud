import 'dart:html';

import 'package:flutter/material.dart';

class iug extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("MY NAMES IS MOHMMED"),
      ),
      body: Center(
        child: const Text("hello to iug course"),
      ),
    );
  }
}
